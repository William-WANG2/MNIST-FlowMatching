name: mnist-flow-matching
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - matplotlib
  - tqdm
  - pip
  - pip:
      - hydra-core>=1.3
      - omegaconf>=2.3
      - einops>=0.7
      - -e .

