defaults:
  - data: mnist
  - task: flow_matching
  - representation: pixel
  - path: gaussian
  - backbone: dit
  - conditioning: cfg
  - simulator: ode
  - vae: default
  - sampling: default
  - _self_

experiment:
  name: mnist_sampling

hydra:
  run:
    dir: .
  output_subdir: null
