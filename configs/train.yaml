defaults:
  - data: mnist
  - path: gaussian
  - backbone: dit
  - conditioning: cfg
  - simulator: ode
  - training: default
  - _self_

experiment:
  name: mnist_flow_matching

hydra:
  run:
    dir: .
  output_subdir: null

