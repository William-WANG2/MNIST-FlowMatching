defaults:
  - data: mnist
  - task: flow_matching
  - representation: pixel
  - path: gaussian
  - backbone: dit
  - conditioning: cfg
  - simulator: ode
  - vae: default
  - training: default
  - _self_

experiment:
  name: mnist_flow_matching

hydra:
  run:
    dir: .
  output_subdir: null
